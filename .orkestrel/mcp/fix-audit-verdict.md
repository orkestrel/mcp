# mcp fix-unit audit — Opus 5 reviewer (cross-engine), 2026-08-20

Subject: the Sol fix unit at commit b59b80d. Summary of the returned verdict; per-claim evidence
is in the session record.

- MR1/MR17 sweeps: BROKEN on the `e.g.` branch — the pasted pattern closes the alternation with
  `\b`, which never matches after the final period, so `e.g.` survives unruled across authored
  TSDoc (the site list rides in the fix2 brief). Every kept `SHOULD`/`MAY` hit is a genuine quoted
  protocol requirement; the third-person conversion held.
- Renames and deletions: CONFIRMED (residue only in campaign records; `on` gone from contract,
  implementation, guide table, and every call site; predicates moved verbatim and lawfully).
- MR6 selection: CONFIRMED against RFC 6455 (absent, multi-token, and whitespace cases handled;
  the proofs assert the whole outcome object). The Orchestrator's mutation probe then bound the
  proofs to the defect: unconditional echo restored at 20:47:07 ran the missing-offer and
  mismatched-offer proofs red (2 failed | 1 passed) with the matching-offer control green;
  restore at 20:47:33 ran 3 passed; the working tree diffed clean after the restore.
- Guide and README truth-claims: CONFIRMED, with the link row settled by the Orchestrator against
  the pushed main: the guide, its `#declared-packaging-limits` anchor, and the LICENSE resolve;
  `CHANGELOG.md` is absent, so README:77 is a dead link.
- Report honesty: BROKEN on the unqualified "No prohibited-sense hit remains" (false of `src/` for
  `e.g.`) and a banned count of the kept hits.

Findings outside the claims: F1 the browser test title and comment still state the removed
unconditional echo; F2 "the five `.map` files" counts in README and the guide; F3 the new
`ROADMAP.md` is the dispatch's own deviation — mcp kept none, so the deferred rename dissolves
into the guide's declared-gaps prose and the file goes.

Referrals ruled by the Orchestrator: R1 the README link gate's population is empty after the
rewrite — recast the assertion over the full link set; R2 one executed transcription pins the
repaired fence class, the wider fence-gating expansion is recorded as a declared gap; R3 settled
by the mutation probe recorded here.

VERDICT: FAIL — 2 broken, 0 unresolved, 1 not-evidenced, 3 findings outside the claims
