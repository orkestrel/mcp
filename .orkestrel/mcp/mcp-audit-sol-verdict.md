1. **BROKEN** — Interleaving `start()` → caller attaches `data` listener → `close()` leaves the caller listener installed but pauses the stream (`src/server/transports/StdioServerTransport.ts:79,115-119`). A second probe showed that a caller-started, listenerless flow is also misclassified. Record pre-start flow state and pause only when the transport started a non-flowing input and no caller reader remains.

2. **BROKEN** — The named caller pattern fails: after `start()`, attaching a `data` listener does not protect the caller because `close()` forces `readableFlowing` to `false`. The executed probe received `false` with the caller listener still attached. Apply the release-time ownership rule from claim 1.

3. **BROKEN** — The universal is too broad. Public `sendEventStream(stream, sse)` accepts a supplied `StreamInterface` and always calls `sse.end()` (`src/server/helpers.ts:70-91`). That call is correct because the helper owns completion of that SSE body; caller-owned stdio streams remain free of `destroy`, `end`, and `removeAllListeners`. Narrow the claim to caller-owned injected stdio streams.

4. **CONFIRMED** — I attacked explicit close, input-driven close, repeated close, and reentrant input close during `removeListener`. The reentrant probe emitted one transport close, preserved the caller listener counts, and removed only `#data`, `#ending`, and `#failure`. Both paths set `#closed` before synchronous release (`src/server/transports/StdioServerTransport.ts:89-119`).

5. **BROKEN** — Reversing the order produces no observable difference. Transport close synchronously invokes the binder’s closed handler, which aborts and clears live requests; `unbind()` performs the same shutdown before installing no-op handlers (`src/core/helpers.ts:1114-1125`). Retain unbinding as cleanup, but remove the unsupported order-matters invariant.

6. **BROKEN** — The six-transport audit found remaining doors. Stdio server has the flow defect above. Stdio client calls `Process.destroy()`, although the installed contract permits its `lines` iteration to remain outstanding. HTTP client leaves an active fetch and response reader alive: an executed never-ending SSE probe observed zero cancellations and an unsettled `send()` after `close()` resolved (`src/server/transports/HTTPClientTransport.ts:106-142`). HTTPDisconnect releases its timer and abort listener. WebSocket client retains no reference to destroy a pending upgrade request (`src/server/transports/WebSocketClientTransport.ts:103-170`). WebSocket server delegates established-socket teardown to the bounded wrapper. Track and abort in-flight HTTP and WebSocket-open resources during close.

7. **BROKEN** — `start(); input.on('data', caller); close()` worked against 0.0.19 because close never paused the input. This tip leaves the caller handler installed but stops its flow. The published control remained flowing; the tip probe did not. Fix and pin the post-start attachment ordering.

8. **BROKEN** — The `unbind()` instrument does not bind. With only `unbind()` removed, the added factory test still passed: 1 passed and 36 skipped. Its assertions observe transport close, listener counts, `destroyed`, and `writableEnded`, but no unbind effect (`tests/src/server/factories.test.ts:706`). Add a control that distinguishes binder detachment through the public composite lifecycle, or redesign close to own that detachment directly.

9. **BROKEN** — The guide’s universal says transport close ends the connection and releases resources (`guides/mcp.md:2776-2785`), but the active HTTP probe disproves it. The guide also says child stderr inherits the parent (`guides/mcp.md:2214-2217`), while `StdioClientTransport` uses `Process`, which pipes and retains bounded stderr evidence. Repair lifecycle teardown, correct the stderr statement, and document stdio ownership behavior.

10. **CONFIRMED** — I attacked the built artifact through an actual pack and install outside the repository. The tarball contained all 18 expected files. Installed ESM entries loaded with 140 core, 19 browser, and 44 server exports; CJS loaded with 140 core and 44 server exports. TypeScript compiler inspection found every runtime export set equal to its `.d.ts` value declarations, and an added outside-population export made the parity control fail.

11. **BROKEN** — I would not publish 0.0.20. Caller-owned flow can be seized, HTTP and pending WebSocket opens can survive close, the unbind repair lacks a binding instrument, and the guide contradicts runtime behavior. This tree cannot establish whether `@orkestrel/probe` exercises the repaired path. The only imported `@orkestrel/process` symbols are `Process` and `PROCESS_GRACE`. Repair these claims and run a successor audit before publication.

VERDICT: FAIL — 9 broken, 0 unresolved, 0 not-evidenced, 0 findings outside the claims