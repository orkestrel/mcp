# HS-U2 integration micro-edit

The parity gate on the HS-U2 tree failed one executed fence: the guide's bare-server paragraph
and its transcription in the guides test pinned the pre-split refusal for a version-less
registered method. The update-every-consumer law lands the consumer update with the behavior
change, so the Orchestrator rewrote the paragraph to the split the code now enforces (registered
method → declaration-missing `-32602`; unregistered method → `-32601` naming it; version key
with failing grammar → the malformed answer unchanged) and moved the transcription's expected
message to the declaration-missing answer. The fuller prose rewrite stays with the
examples-and-guide unit. This edit is Orchestrator-written, so the closing cross-engine audit
names it as a claim.

Failing assertion, 2026-08-20: `tests/guides.test.ts > guides/mcp.md tools/list request
metadata > requires modern metadata before the guide request succeeds`, expected the malformed
message and received `Invalid params: request declares no protocol version`.
