# Handshake wave audit: verdicts and reconciliation

Subject: commit b404a84 in the pinned worktree, with the browser follow-through a42d34f landing
after the worktree was cut. Lanes: Sol `analyst` (claims 1-5, journal
`tmp/codex/hs-wave-analyst-journal.jsonl`, session `01a02182-7f1d-7bf2-973e-54e124ed6481`) and
Opus `reviewer` (claims 6-8, native). Both returned `VERDICT: FAIL`, 2026-08-20. The lanes' full
answers ride beside this file.

## Lane verdicts

- Claim 1 BROKEN (Sol) - the packed matrix's 15s row never observes the applied deadline, and
  the pinned rows' conforming peers cannot catch a client that accepts a lying peer's differing
  revision; the construction row and the red control are strong.
- Claim 2 CONFIRMED (Sol) - the stdio and WebSocket controls bind the split; the pre-split
  answer fails them.
- Claim 3 BROKEN (Sol) - the flagship fence discriminates, and the sweep missed the worker
  bootstrap: `serveMCPScope` binds a bare `MCPServer` with no legacy seam while the guide calls
  `serveMCP` a drop-in exposing the registry to every client, unlabeled.
- Claim 4 CONFIRMED (Sol) - every Orchestrator micro-edit matches the landed code.
- Claim 5 BROKEN (Sol) - the tree-wide text assertions fail on retained campaign records and the
  guide's truthful kept-refusal sentence, and the claim inherits the claim-1 and claim-3 gaps.
- Claim 6 CONFIRMED (Opus) - the pin contract holds every design law attacked, with the context
  shapes agreeing with the guide by key and branch.
- Claim 7 CONFIRMED (Opus) - the split is honest at each answer, the guide states the exact
  discriminator, and `MCPServer.ts` carries no era spelling.
- Claim 8 CONFIRMED (Opus) - the touched prose keeps the canon; the sweep's hits are the
  permitted sense in pre-existing text.

## Orchestrator rulings

- Claim 3: sustained as the wave's one product finding. Carrier: HS-U5 labels `serveMCP` and
  `serveMCPScope` modern-only on their TSDoc, surface row, and guide prose, and directs dual-era
  users to the `bindServer(createMCPLegacy(...))` composition. Whether `ServeMCPOptions` gains a
  dispatcher seam is an API question recorded against the worker-serving capability for a
  successor change, not this wave.
- Opus F1: sustained as required. Carrier: HS-U5 replaces the `MCPError` surface row with the
  reviewer's prescribed sentence. The dropped carrier was the Orchestrator's: the design lane
  assigned the row, the HS-U1 brief carried only the source half with the guide off-limits, and
  no later brief picked the row up.
- Claim 1: sustained only as label honesty. The pin-branch defects the lane names are bound
  red-then-green by the HS-U1 core proofs - the lying-peer legacy mismatch, the
  discovery-omitting modern pin, and the applied probe deadline each ran red before the fix in
  `tests/src/core/MCPClient.test.ts` - so the packed matrix's subject stays end-to-end
  negotiation of the installed artifact. Carrier: HS-U5 renames the 15s row to claim the
  negotiation outcome rather than deadline fidelity and adds one comment naming where the pin
  branches are bound.
- Claim 5: the population was the audit brief's defect, not the tree's. Retained campaign
  records keep their historical spellings verbatim as evidence, and the guide's malformed-grammar
  sentence states kept behavior. The product-bearing population - `src/`, `guides/`, `README.md`,
  `tests/` - carries no deleted-constant spelling and no pre-split absent-declaration message
  outside the kept sites, which the lane's own reading shows. No carrier.
- Opus F2: adopted. HS-U5 names `initialize` in the modern-only marginal note.
- Opus F3: recorded, not changed. The comparison is equivalent today, the code is Sol-written
  and confirmed, and reopening an audited surface for a spelling preference is churn.
- The reviewer's referral (import ordering): the toolchain instruments report 0 and own the
  question; no action.
- The browser capture-gate expectation (a42d34f) postdates the pinned worktree: the mcp
  verifier's full chain surfaced it, the fix follows the pinned message, and the re-run verifier
  read every gate 0 at a42d34f. It belongs to the same consumer-update class as claim 4's
  micro-edits.

## Terminal

VERDICT: FAIL - carriers HS-U5 (claim 3, Opus F1, the claim-1 label, Opus F2).
